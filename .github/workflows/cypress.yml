name: Cypress Tests
on: [push]
jobs:
  cypress-run:
    runs-on: ubuntu-22.04
    steps:
      - name: Checkout
        uses: actions/Checkout@v3
      - name: Cypress run
        uses: cypress-io/github-actions@v5